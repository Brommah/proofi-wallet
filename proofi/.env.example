# =============================================================================
# PROOFI ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# For Vercel deployment, set these in your Vercel project dashboard:
#   Vercel Dashboard → Project Settings → Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# CERE DDC (Decentralized Data Cloud) - REQUIRED
# -----------------------------------------------------------------------------
# These are needed for decentralized storage functionality.

# Wallet JSON - The contents of your wallet.json file (as a single-line JSON string)
# Get this from: https://wallet.cere.network
# Export your wallet → Copy the JSON contents → Paste as a single line
# Example: {"encoded":"...","encoding":{...},"address":"5DSx...","meta":{}}
DDC_WALLET_JSON=

# Wallet password - The password you set when creating/exporting your wallet
# Keep this secret! Never commit real passwords to git.
DDC_WALLET_PASSWORD=

# Bucket ID - Your DDC storage bucket ID
# Get this by creating a bucket in the Cere Developer Console
# or via the SDK: client.createBucket(...)
# Testnet buckets start around 1000+
DDC_BUCKET_ID=1229

# -----------------------------------------------------------------------------
# UPSTASH REDIS (Optional - for caching/rate limiting)
# -----------------------------------------------------------------------------
# Sign up at: https://upstash.com (free tier available)
# Create a Redis database → Copy REST URL and Token

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# -----------------------------------------------------------------------------
# AI PROVIDERS (Optional - for health analyzer agent)
# -----------------------------------------------------------------------------
# Only needed if running the Health Analyzer agent
# Choose ONE provider (OpenAI OR Anthropic)

# OpenAI API Key - https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic API Key - https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# DEVELOPMENT
# -----------------------------------------------------------------------------
# Set automatically by Vercel, but useful for local debugging

NODE_ENV=development

# Log level: debug, info, warn, error
LOG_LEVEL=info
