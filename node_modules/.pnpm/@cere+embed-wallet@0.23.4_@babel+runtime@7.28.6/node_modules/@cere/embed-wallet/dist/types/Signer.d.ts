import { ProviderInterface, SignerInterface } from './Provider';
import type { WalletAccount, WalletAccountType } from './types';
export type SignerOptions = {
    accountIndex?: number;
    address?: string;
    type?: WalletAccountType;
};
export declare class Signer implements SignerInterface {
    readonly provider: ProviderInterface;
    readonly options: SignerOptions;
    constructor(provider: ProviderInterface, options?: SignerOptions);
    getAccount(): Promise<WalletAccount>;
    getAddress(): Promise<string>;
    signMessage(message: string): Promise<string>;
}
