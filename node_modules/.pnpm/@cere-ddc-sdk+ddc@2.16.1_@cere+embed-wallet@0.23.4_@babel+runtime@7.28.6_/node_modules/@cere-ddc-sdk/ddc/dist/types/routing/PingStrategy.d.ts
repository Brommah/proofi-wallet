import { RouterOperation, RouterNode } from '.';
import { NodeTypeStrategy } from './NodeTypeStrategy';
/**
 * The `PingStrategy` selects a node based on the operation type and ping latency.
 */
export declare abstract class PingStrategy extends NodeTypeStrategy {
    private nodesMap;
    private getPingedNodes;
    private ping;
    private enqueuePing;
    marshalNodes(operation: RouterOperation, allNodes: RouterNode[]): Promise<RouterNode[]>;
}
//# sourceMappingURL=PingStrategy.d.ts.map