import { RouterNode, RouterOperation, RoutingStrategy } from './RoutingStrategy';
export declare abstract class BaseStrategy extends RoutingStrategy {
    marshalNodes(operation: RouterOperation, allNodes: RouterNode[]): Promise<RouterNode[]>;
}
//# sourceMappingURL=BaseStrategy.web.d.ts.map