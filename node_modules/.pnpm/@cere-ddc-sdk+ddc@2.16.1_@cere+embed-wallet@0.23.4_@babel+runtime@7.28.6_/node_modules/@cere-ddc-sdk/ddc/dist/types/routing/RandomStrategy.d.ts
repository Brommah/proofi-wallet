import { StorageNodeConfig } from '..';
import { BaseStrategy } from './BaseStrategy';
import { RouterOperation, RouterNode } from './RoutingStrategy';
export declare const shuffle: (array: RouterNode[]) => RouterNode[];
export declare abstract class RandomStrategy extends BaseStrategy {
    marshalNodes(operation: RouterOperation, nodes: StorageNodeConfig[]): Promise<StorageNodeConfig[]>;
    selectNode(operation: RouterOperation, nodes: RouterNode[]): RouterNode;
}
//# sourceMappingURL=RandomStrategy.d.ts.map