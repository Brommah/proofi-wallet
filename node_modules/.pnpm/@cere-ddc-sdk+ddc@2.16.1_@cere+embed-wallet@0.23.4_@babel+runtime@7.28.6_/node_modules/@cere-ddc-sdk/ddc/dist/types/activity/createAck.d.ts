import type { Signer } from '@cere-ddc-sdk/blockchain';
import type { Logger } from '../logger';
import { ActivityAcknowledgment } from '../grpc/activity_report/activity_report';
import { CreateSignatureOptions } from '../signature';
export type CreateAckOptions = CreateSignatureOptions & {
    signer?: Signer;
    logger?: Logger;
};
/**
 * Creates an activity acknowledgment.
 */
export declare const createAck: (ack: Omit<ActivityAcknowledgment, "signature">, { signer, logger, ...options }: CreateAckOptions) => Promise<ActivityAcknowledgment>;
//# sourceMappingURL=createAck.d.ts.map