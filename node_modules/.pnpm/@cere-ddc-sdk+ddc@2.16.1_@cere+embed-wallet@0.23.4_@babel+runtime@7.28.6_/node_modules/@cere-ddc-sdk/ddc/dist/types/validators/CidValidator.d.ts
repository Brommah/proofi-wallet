import { Logger } from '../logger';
export type CidValidatorOptions = {
    logger?: Logger;
    enable?: boolean;
};
export declare class CidValidator {
    private hasherPromise?;
    private cid;
    protected logger?: Logger;
    protected readonly enable: boolean;
    constructor(cid: Uint8Array, { logger, enable }?: CidValidatorOptions);
    protected getHasher(): Promise<import("hash-wasm").IHasher>;
    update(chunk: Uint8Array): Promise<this>;
    validate(): Promise<void>;
}
//# sourceMappingURL=CidValidator.d.ts.map