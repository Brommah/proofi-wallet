import type { Signer } from '@cere-ddc-sdk/blockchain';
import { Signature } from '../grpc/common/signature';
import { AuthToken } from '../auth';
export type ApiSignature = Signature;
export type CreateSignatureOptions = {
    token?: string | AuthToken;
};
export declare const createSignature: (signer: Signer, message: Uint8Array, { token }?: CreateSignatureOptions) => Promise<Signature>;
//# sourceMappingURL=createSignature.d.ts.map