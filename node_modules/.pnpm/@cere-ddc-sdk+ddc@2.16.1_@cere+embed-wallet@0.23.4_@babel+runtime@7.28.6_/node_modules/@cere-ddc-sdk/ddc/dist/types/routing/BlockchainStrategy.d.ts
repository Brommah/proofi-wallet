import { Blockchain, BucketId } from '@cere-ddc-sdk/blockchain';
import { RouterNode } from './RoutingStrategy';
import { Logger } from '../logger';
import { PingStrategy } from './PingStrategy';
export type BlockchainStrategyConfig = {
    blockchain: Blockchain;
};
/**
 * The `BlockchainStrategy` retrieves nodes from the blockchain. And appiles the `PingStrategy` + `NodeTypeStrategy` to select the best node.
 */
export declare class BlockchainStrategy extends PingStrategy {
    private blockchain;
    private bucketCache;
    private clusterNodes;
    constructor(logger: Logger, { blockchain }: BlockchainStrategyConfig);
    isReady(): Promise<boolean>;
    getNodes(bucketId: BucketId): Promise<RouterNode[]>;
    private mapNodeProps;
    private getClusterNodes;
    private getBucket;
}
//# sourceMappingURL=BlockchainStrategy.d.ts.map