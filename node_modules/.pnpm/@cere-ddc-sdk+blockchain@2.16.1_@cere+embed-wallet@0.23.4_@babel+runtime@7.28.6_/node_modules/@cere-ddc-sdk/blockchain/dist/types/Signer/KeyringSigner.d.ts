import { Signer as BcSigner } from '@polkadot/types/types';
import { KeyringPair } from '@polkadot/keyring/types';
import { Signer } from './Signer';
/**
 * Signer that uses a keyring pair to sign data.
 *
 * @group Signers
 * @extends Signer
 * @example
 *
 * ```typescript
 * const pair = new Keyring().addFromUri('//Alice');
 * const keyringSigner = new KeyringSigner(pair);
 * const signature = await keyringSigner.sign('data');
 *
 * console.log(signature);
 * ```
 */
export declare class KeyringSigner extends Signer {
    private pair?;
    constructor(pair?: KeyringPair | undefined);
    get isLocked(): boolean;
    unlock(passphrase?: string): Promise<void>;
    protected createPair(): KeyringPair;
    isReady(): Promise<boolean>;
    private getPair;
    get type(): import("@polkadot/util-crypto/types").KeypairType;
    get address(): string;
    get publicKey(): Uint8Array<ArrayBufferLike>;
    sign(data: Uint8Array | string): Promise<Uint8Array<ArrayBufferLike>>;
    getSigner(): Promise<BcSigner>;
}
//# sourceMappingURL=KeyringSigner.d.ts.map