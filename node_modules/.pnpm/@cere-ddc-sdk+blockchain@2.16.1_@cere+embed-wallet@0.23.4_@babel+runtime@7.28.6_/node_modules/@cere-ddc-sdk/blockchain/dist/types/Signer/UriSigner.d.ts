import { KeyringPair, KeyringOptions } from '@polkadot/keyring/types';
import { KeyringSigner } from './KeyringSigner';
export type UriSignerOptions = Pick<KeyringOptions, 'type'>;
/**
 * Signer that uses a Substrate URI to create a keypair.
 *
 * @group Signers
 * @extends Signer
 * @example
 *
 * ```typescript
 * const uriSigner = new UriSigner('//Alice', );
 * const signature = await uriSigner.sign('data');
 *
 * console.log(signature);
 * ```
 */
export declare class UriSigner extends KeyringSigner {
    private uri;
    private options;
    constructor(uri: string, options?: UriSignerOptions);
    protected createPair(): KeyringPair;
    get type(): import("@polkadot/util-crypto/types").KeypairType;
    static create(uri: string, options?: UriSignerOptions): Promise<UriSigner>;
}
//# sourceMappingURL=UriSigner.d.ts.map