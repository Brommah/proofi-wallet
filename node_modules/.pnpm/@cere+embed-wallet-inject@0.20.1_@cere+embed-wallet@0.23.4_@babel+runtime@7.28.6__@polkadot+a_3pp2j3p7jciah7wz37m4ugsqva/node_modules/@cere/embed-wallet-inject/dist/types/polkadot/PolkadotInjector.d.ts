import { EmbedWallet, WalletConnectOptions } from '@cere/embed-wallet';
import type { Injected, InjectedAccount } from '@polkadot/extension-inject/types';
import type { SignerPayloadRaw, SignerResult, SignerPayloadJSON } from '@polkadot/types/types';
export type PolkadotInjectorOptions = {
    name?: string;
    version?: string;
    autoConnect?: boolean;
    waitReady?: boolean;
    connectOptions?: WalletConnectOptions;
};
export declare class PolkadotInjector {
    readonly wallet: EmbedWallet;
    private name;
    private version;
    private injected;
    private shouldConnect;
    private shouldWait;
    private connectOptions?;
    constructor(wallet: EmbedWallet, { name, version, autoConnect, waitReady, connectOptions }?: PolkadotInjectorOptions);
    get isInjected(): boolean;
    private waitReady;
    private filterAccounts;
    readonly getAccounts: () => Promise<InjectedAccount[]>;
    readonly subscribeAccounts: (onReceive: (accounts: InjectedAccount[]) => void) => () => void;
    readonly signRaw: (raw: SignerPayloadRaw) => Promise<SignerResult>;
    readonly signPayload: (payload: SignerPayloadJSON) => Promise<SignerResult>;
    readonly enable: () => Promise<Injected>;
    inject(): void;
}
