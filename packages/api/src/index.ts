/**
 * @proofi/api — Auth API server
 *
 * Responsibilities:
 * - OTP email authentication
 * - Telegram auth (Mini App + Login Widget)
 * - JWT issuance via JWKS
 * - Web3Auth key retrieval (server-side)
 * - Lightweight — Hono or Fastify, not NestJS
 */

export const VERSION = '0.0.1';
